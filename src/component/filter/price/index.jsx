import React from 'react';
import PropTypes from 'prop-types';

Price.propTypes = {
    
};

function Price(props) {
    return (
        <div>
            
        </div>
    );
}

export default Price;